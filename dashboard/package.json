{
  "name": "wonton-dashboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "wasm:build": "wasm-pack build --target web --out-dir ../../dashboard/src/wasm-pkg ../crates/wasm",
    "dev": "npm run wasm:build && vite",
    "build": "npm run wasm:build && tsc -b && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
