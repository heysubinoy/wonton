# Copy to .env for local dev if wonton-server runs on a different origin than the dashboard's
# dev server (e.g. `cargo run -p wonton-server` on :8080, `npm run dev` on :5173).
WONTON_SERVER_URL=http://localhost:8080
